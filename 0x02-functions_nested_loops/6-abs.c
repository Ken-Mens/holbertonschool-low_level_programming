#include "holberton.h"
/**
  * _abs - computes absolute value of an integer
  * @r: integer parameter
  * Return: 0 on success.
  */
int _abs(int r)
{
	if (r < 0)
	{
		return (r * -1);
	}
	else
	return (r);
}
